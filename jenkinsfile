pipeline {
    agent any
    stages {
        stage('fetch') {
            steps {
                sh 'echo git'
            }
        }
        stage('build') {
            steps {
                sh 'echo longfile'
            }
        }
        
    }
}